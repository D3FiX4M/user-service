spring:
  main:
    banner-mode: "off"
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: validate
  datasource:
    url: jdbc:postgresql://localhost:5432/user_service
    username: postgres
    password: admin


server:
  port: 8082

instance:
  key: user-service
  id: 1

grpc:
  enable-reflection: true
  port: 9092
  security:
    auth:
      enabled: true





